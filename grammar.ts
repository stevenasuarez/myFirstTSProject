var x = 1;

var firstName = 'Steven';
var num1 = 100;
var num2: number = 20;

function addNumber(n1: number, n2: number, n3: number) {
    var result = n1 + n2 + n3;
    var msg = 'Sum is = ' + result;
    alert(msg);
}
addNumber(num1, num2, x);